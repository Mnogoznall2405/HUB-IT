# Playwright `/chat` smoke — backlog

Staging E2E for chat is intentionally not in `test:chat:gate` yet.

Planned checks when Playwright is wired for this repo:

- login → navigate `/chat` → sidebar renders
- open conversation → thread header + composer mount
- send text message (mock API or staging tenant)

Track under wave E in the chat post-audit roadmap.
