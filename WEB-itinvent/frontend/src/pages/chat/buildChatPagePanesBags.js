export default function buildChatPagePanesBags(input) {
  return {
    shell: {
      theme: input.theme,
      ui: input.ui,
      isMobile: input.isMobile,
      isPhone: input.isPhone,
      mobileMotionDisabled: input.mobileMotionDisabled,
    },
    sidebar: {
      health: input.health,
      user: input.user,
      unreadTotal: input.unreadTotal,
      sidebarQuery: input.sidebarQuery,
      setSidebarQuery: input.setSidebarQuery,
      sidebarSearchActive: input.sidebarSearchActive,
      searchingSidebar: input.searchingSidebar,
      searchPeople: input.searchPeople,
      searchChats: input.searchChats,
      searchResultEmpty: input.searchResultEmpty,
      openingPeerId: input.openingPeerId,
      handleOpenPeer: input.handleOpenPeer,
      activeConversationId: input.activeConversationId,
      openConversation: input.openConversation,
      prefetchThreadBootstrap: input.prefetchThreadBootstrap,
      conversationsLoading: input.conversationsLoading,
      filteredConversations: input.filteredConversations,
      openGroupDialog: input.openGroupDialog,
      sidebarScrollRef: input.sidebarScrollRef,
      handleSidebarScroll: input.handleSidebarScroll,
      conversationFilter: input.conversationFilter,
      handleActiveFolderChange: input.handleActiveFolderChange,
      customFolders: input.customFolders,
      conversationFilterCounts: input.conversationFilterCounts,
      conversationIdsByFolder: input.conversationIdsByFolder,
      handleOpenFolderManager: input.handleOpenFolderManager,
      handleOpenArchiveFolder: input.handleOpenArchiveFolder,
      handleToggleConversationInFolder: input.handleToggleConversationInFolder,
      draftsByConversation: input.draftsByConversation,
      updateConversationSettings: input.updateConversationSettings,
      requestDeleteConversation: input.requestDeleteConversation,
      requestLeaveConversation: input.requestLeaveConversation,
      conversationActionPendingId: input.conversationActionPendingId,
      aiSidebarRows: input.aiSidebarRows,
      aiBotsLoading: input.aiBotsLoading,
      aiBotsError: input.aiBotsError,
      canUseAiChat: input.canUseAiChat,
      handleOpenAiBot: input.handleOpenAiBot,
      openingAiBotId: input.openingAiBotId,
    },
    thread: {
      activeConversation: input.activeConversation,
      navigate: input.navigate,
      threadWallpaperSx: input.threadWallpaperSx,
      messages: input.messages,
      messagesLoading: input.messagesLoading,
      effectiveLastReadMessageId: input.effectiveLastReadMessageId,
      showOlderHistoryControl: input.showOlderHistoryControl,
      loadingOlder: input.loadingOlder,
      prependScrollRestoreRef: input.prependScrollRestoreRef,
      loadOlderMessages: input.loadOlderMessages,
      threadScrollRef: input.threadScrollRef,
      threadContentRef: input.threadContentRef,
      handleThreadScroll: input.handleThreadScroll,
      bottomRef: input.bottomRef,
      openMobileInboxView: input.openMobileInboxView,
      handleOpenInfo: input.handleOpenInfo,
      openTaskFromChat: input.openTaskFromChat,
      openSearchDialog: input.openSearchDialog,
      handleOpenMenu: input.handleOpenMenu,
      openMessageReads: input.openMessageReads,
      openMediaViewer: input.openMediaViewer,
      handleReplyMessage: input.handleReplyMessage,
      openMessageMenu: input.openMessageMenu,
      confirmAiAction: input.confirmAiAction,
      cancelAiAction: input.cancelAiAction,
      editAiAction: input.editAiAction,
      selectedVisibleMessageIds: input.selectedVisibleMessageIds,
      selectedMessageCount: input.selectedMessageCount,
      canCopySelectedMessages: input.canCopySelectedMessages,
      toggleMessageSelection: input.toggleMessageSelection,
      startMessageSelection: input.startMessageSelection,
      clearSelectedMessages: input.clearSelectedMessages,
      selectedReplyToSelectedMessage: input.selectedReplyToSelectedMessage,
      selectedCopySelectedMessages: input.selectedCopySelectedMessages,
      selectedOpenForwardSelectedMessages: input.selectedOpenForwardSelectedMessages,
      handleOpenComposerMenu: input.handleOpenComposerMenu,
      composerRef: input.composerRef,
      messageText: input.messageText,
      setMessageText: input.setMessageText,
      handleComposerKeyDown: input.handleComposerKeyDown,
      syncComposerSelection: input.syncComposerSelection,
      handleOpenEmojiPicker: input.handleOpenEmojiPicker,
      handleCloseEmojiPicker: input.handleCloseEmojiPicker,
      handleComposerFocusChange: input.handleComposerFocusChange,
      handleComposerSend: input.handleComposerSend,
      handleComposerPaste: input.handleComposerPaste,
      handleComposerDrop: input.handleComposerDrop,
      handleComposerDragOver: input.handleComposerDragOver,
      handleComposerDragLeave: input.handleComposerDragLeave,
      mentionCandidates: input.mentionCandidates,
      searchMentionPeople: input.searchMentionPeople,
      fileDragActive: input.fileDragActive,
      showJumpToLatest: input.showJumpToLatest,
      jumpToLatest: input.jumpToLatest,
      replyMessage: input.replyMessage,
      clearReplyMessage: input.clearReplyMessage,
      editingMessage: input.editingMessage,
      clearEditingMessage: input.clearEditingMessage,
      aiTypingStatus: input.aiTypingStatus,
      activeAiStatus: input.activeAiStatus,
      pinnedMessage: input.pinnedMessage,
      handleOpenPinnedMessage: input.handleOpenPinnedMessage,
      handleUnpinPinnedMessage: input.handleUnpinPinnedMessage,
      highlightedMessageId: input.highlightedMessageId,
      conversationMetaSubtitle: input.conversationMetaSubtitle,
      aiAwareTypingLine: input.aiAwareTypingLine,
      renderDesktopRightPanel: input.renderDesktopRightPanel,
      selectedFiles: input.selectedFiles,
      fileCaption: input.fileCaption,
      openFilePicker: input.openFilePicker,
      clearSelectedFiles: input.clearSelectedFiles,
      preparingFiles: input.preparingFiles,
      sendingFiles: input.sendingFiles,
      fileUploadProgress: input.fileUploadProgress,
      selectedFilesSummary: input.selectedFilesSummary,
      getReadTargetRef: input.getReadTargetRef,
      handleToggleReaction: input.handleToggleReaction,
      scrollToMessage: input.scrollToMessage,
      emojiPickerOpen: input.emojiPickerOpen,
      insertEmojiAtSelection: input.insertEmojiAtSelection,
      handleSendGif: input.handleSendGif,
      voiceRecording: input.voiceRecording,
      voiceRecordingDuration: input.voiceRecordingDuration,
      voiceRecordingLevelRef: input.voiceRecordingLevelRef,
      startVoiceRecording: input.startVoiceRecording,
      stopVoiceRecording: input.stopVoiceRecording,
      cancelVoiceRecording: input.cancelVoiceRecording,
      bindPinnedScroll: input.bindPinnedScroll,
      activeConversationId: input.activeConversationId,
    },
    rightPanel: {
      showTaskPanel: input.showTaskPanel,
      showContextPanel: input.showContextPanel,
      taskPanelTaskId: input.taskPanelTaskId,
      closeTaskPanel: input.closeTaskPanel,
      openTaskInTasks: input.openTaskInTasks,
      handleTaskPanelUpdated: input.handleTaskPanelUpdated,
      setContextPanelOpen: input.setContextPanelOpen,
      openShareDialog: input.openShareDialog,
      handleAddGroupMembers: input.handleAddGroupMembers,
      handleRemoveGroupMember: input.handleRemoveGroupMember,
      handleUpdateGroupMemberRole: input.handleUpdateGroupMemberRole,
      handleTransferGroupOwnership: input.handleTransferGroupOwnership,
      handleLeaveGroup: input.handleLeaveGroup,
      handleUpdateGroupProfile: input.handleUpdateGroupProfile,
      settingsUpdating: input.settingsUpdating,
      socketStatus: input.socketStatus,
      conversationMetaSubtitle: input.conversationMetaSubtitle,
      activeConversation: input.activeConversation,
      messages: input.messages,
      openSearchDialog: input.openSearchDialog,
      openFilePicker: input.openFilePicker,
      updateConversationSettings: input.updateConversationSettings,
      openMediaViewer: input.openMediaViewer,
      user: input.user,
      navigate: input.navigate,
      theme: input.theme,
      ui: input.ui,
    },
  };
}
